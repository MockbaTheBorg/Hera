# Hera - Hercules Hyperion GUI - by Mockba the Borg
#
"""Hera scripting API — an embedded HTTP server that lets scripts operate
Hera itself (select devices, type into consoles/terminals, turn dials, mount
tapes, change appearance) the same way a human would, without going through
the Hercules backend directly. See app/scripting/server.py.
"""
